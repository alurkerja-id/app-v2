// Signed-in demo user. Deliberately long so the header avatar menu keeps its
// truncation behaviour visible in the prototype.
export const currentUser = {
  name: "Alice Wonderland McPherson",
  email: "alice.wonderland.mcpherson@internationalcorporation.com",
  initials: "AW",
}
